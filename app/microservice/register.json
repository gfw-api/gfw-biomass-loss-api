{
    "id": "#(service.id)",
    "name": "#(service.name)",
    "tags": ["gfw"],
    "urls": [{
        "url": "/v1/biomass-loss/admin/:iso",
        "method": "GET",
        "endpoints": [{
            "method": "GET",
            "baseUrl": "#(service.uri)",
            "path": "/api/v1/biomass-loss/admin/:iso"
        }]
    }, {
        "url": "/v1/biomass-loss/admin/:iso/:id1",
        "method": "GET",
        "endpoints": [{
            "method": "GET",
            "baseUrl": "#(service.uri)",
            "path": "/api/v1/biomass-loss/admin/:iso/:id1"
        }]
    }]
}
